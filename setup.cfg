[tool:pytest]
testpaths = tests
pythonpath = tests
addopts = --cov --cov-report=term-missing

[coverage:run]
source = .
omit =
    tests/*
    .venv/*
    test.py

[coverage:report]
show_missing = true
fail_under = 100
exclude_lines =
    if __name__ == .__main__.
    pragma: no cover
